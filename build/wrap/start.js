 (function(factory){
 	// AMD
 	if( typeof define !== "undefined" && define["amd"] ){
 		define( factory.bind(window) );
 	// No module loader
 	}else{
 		factory(window);
 	}

})(function(_exports){
